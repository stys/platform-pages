# Customizable and extensible wiki module for Play Framework 2.x (java).

## Overview

## Design 

## Integration and customization

The default implementation uses the 

Start by setting up a dependency in the `build.sbt` of your project
```
resolvers += "Snapshots" at "https://raw.github.com/stys/maven-snapshots/master/"

libraryDependencies ++= Seq(
  ...,	
  "com.stys" %% "platform-pages" % "1.0.0-SNAPSHOT",
  javaJdbc,
  javaEbean
)   
```

In the `application.conf` of your project modify the 

## Extend pages

## Contribute


