# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Index
GET     /                                 com.stys.platform.pages.impl.Controller.index()

# Pages
GET     /pages/:namespace/*key            com.stys.platform.pages.impl.Controller.pages(namespace, key)
GET		/pages/create    				  com.stys.platform.pages.impl.Controller.create(namespace ?= null, key ?= null)
GET		/pages/edit					  	  com.stys.platform.pages.impl.Controller.edit(namespace ?= null, key ?= null)
POST	/pages/save						  com.stys.platform.pages.impl.Controller.save

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
