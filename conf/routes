# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Index
GET     /                                 controllers.Application.index()

# Pages
GET     /pages/:namespace/*key            controllers.Application.pages(namespace, key)
GET		/pages/create    				  controllers.Application.create(namespace ?= null, key ?= null)
GET		/pages/editor					  controllers.Application.editor(namespace ?= null, key ?= null)
POST	/pages/edit						  controllers.Application.edit

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
